<UserControl x:Class="Calculator.CustomControl.MainLeftItem"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             TextElement.Foreground="{DynamicResource MaterialDesignBody}"
             TextElement.FontWeight="Regular"
             TextElement.FontSize="13"
             TextOptions.TextFormattingMode="Ideal"
             TextOptions.TextRenderingMode="Auto"
             Background="{DynamicResource MaterialDesignPaper}"
             FontFamily="{DynamicResource MaterialDesignFont}"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Calculator.CustomControl"
             mc:Ignorable="d" Name="mainLeftItem">
    <Button Style="{StaticResource mainLeftButtonItem}"
            Command="{Binding CustomClickCommand}">
        <StackPanel Orientation="Horizontal" Width="{Binding ElementName=mainLeftItem, Path=CustomWidth}">
            <materialDesign:PackIcon Margin="0 0 15 0" Kind="{Binding ElementName=mainLeftItem, Path=CustomIcon}" />
            <TextBlock Text="{Binding ElementName=mainLeftItem, Path=CustomTitle}"/>
        </StackPanel>
    </Button>
</UserControl>
