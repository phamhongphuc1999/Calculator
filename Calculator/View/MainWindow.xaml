<Window x:Class="Calculator.View.MainWindow"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        FontFamily="{DynamicResource MaterialDesignFont}"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Calculator.View"
        xmlns:customcontrol="clr-namespace:Calculator.CustomControl"
        mc:Ignorable="d" WindowStartupLocation="CenterScreen" Name="mainWindow"
        Title="Calculator" Height="650" Width="800" WindowStyle="None" AllowsTransparency="True"
        Icon="pack://application:,,,/Images/main.ico" Topmost="False"
        DataContext="{StaticResource mainViewModel}">
    <Grid>
        <Grid.Resources >
            <Style TargetType="Border" >
                <Setter Property="Margin" Value="0" />
                <Setter Property="Padding" Value="0" />
                <Setter Property="BorderBrush" Value="Transparent"/>
                <Setter Property="BorderThickness" Value="0"/>
            </Style>
        </Grid.Resources>
        <Grid.Background>
            <LinearGradientBrush EndPoint="1,0" StartPoint="0,1">
                <GradientStop Color="#8c8b87" Offset="0"/>
                <GradientStop Color="#ffff" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <customcontrol:CustomControlBar Grid.Row="0" Tag="{Binding ElementName=mainWindow, Path=Title}" />
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="0" Width="165" Background="White">
                <ToggleButton VerticalAlignment="Center" HorizontalAlignment="Center" Width="40" Height="40"
                      BorderThickness="0" IsChecked="True">
                    <ToggleButton.Background>
                        <ImageBrush ImageSource="../Images/tgBtn_default.png" Stretch="None"/>
                    </ToggleButton.Background>
                    <ToggleButton.Triggers>
                        <EventTrigger RoutedEvent="ToggleButton.Unchecked">
                            <BeginStoryboard>
                                <Storyboard x:Name="HideListView">
                                    <DoubleAnimation
                                Storyboard.TargetName="mainLeftSidebar"
                                Storyboard.TargetProperty="Width"
                                BeginTime="0:0:0"
                                From="165" To="0"
                                Duration="0:0:0.2">
                                    </DoubleAnimation>
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                        <EventTrigger RoutedEvent="ToggleButton.Checked">
                            <BeginStoryboard>
                                <Storyboard x:Name="ShowListView">
                                    <DoubleAnimation   
                                Storyboard.TargetName="mainLeftSidebar"
                                Storyboard.TargetProperty="Width"
                                BeginTime="0:0:0"
                                From="0" To="165"
                                Duration="0:0:0.3">
                                    </DoubleAnimation>
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                    </ToggleButton.Triggers>
                </ToggleButton>
            </Grid>
            <customcontrol:CustomSubControlBar Grid.Column="1" Tag="{Binding ElementName=mainWindow, Path=Title}" />
        </Grid>
        
        <Grid Grid.Row="2">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"></RowDefinition>
                    <RowDefinition></RowDefinition>
                </Grid.RowDefinitions>
            </Grid>
            <Grid HorizontalAlignment="Left" Width="auto" Background="Red">
                <customcontrol:MainLeftSideBar Width="165" x:Name="mainLeftSidebar" 
                                               StandardClick="{Binding StandardClickCommand}"/>
            </Grid>
        </Grid>
    </Grid>
</Window>
